-- Run connected to database alexsoft (user alexsoft or postgres).
-- Service schemas (games, rag, …) are added when each app is introduced.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS pgcrypto;
