# alexsoft MinIO demo

Учебный объект в бакете `alexsoft`.

- S3 API: `http://127.0.0.1:9000`
- Console: `http://127.0.0.1:9001`
- Префикс: `demo/docs/`

Этот файл залит скриптом `seed-demo.ps1`. Его же можно открыть в MinIO Console (Preview) и скачать через `mc cat` / `mc cp`.
